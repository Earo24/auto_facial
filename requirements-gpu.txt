# GPU 版本依赖 - 需要 NVIDIA GPU + CUDA
# 使用前请确保已安装 NVIDIA 驱动和 CUDA 工具包

# 核心依赖
numpy>=1.23.0
opencv-python>=4.8.0
insightface>=0.7.3
# GPU 版本的 ONNX Runtime（需要 CUDA）
onnxruntime-gpu>=1.15.0
scikit-learn>=1.3.0

# Web界面
streamlit>=1.28.0
streamlit-option-menu>=0.3.6

# API服务器
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# 可视化
matplotlib>=3.7.0
networkx>=3.1.0
plotly>=5.17.0

# 工具
tqdm>=4.66.0
pillow>=10.0.0
pyyaml>=6.0

# 数据处理
pandas>=2.0.0

# 数据库
sqlalchemy>=2.0.0
